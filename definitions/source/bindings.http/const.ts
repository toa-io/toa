/** What a component answers streamed calls on, where its address states no port of its own. */
export const PORT = 8005

/** The address map of the context, as the deployment writes it into the environment. */
export const VARIABLE = 'TOA_HTTP'

export const ID = 'http'
