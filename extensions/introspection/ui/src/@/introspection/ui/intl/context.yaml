context: Mount 'introspection' context
inputs:
  nodes:
    empty:
      input: Nothing has reported yet.
      context: shown when the map holds no components
    nomatch:
      input: Nothing matches the filter.
      context: shown when the filter leaves the list empty
  node:
    operations:
      input: operation
    events:
      input: event
    receivers:
      input: receiver
    state:
      input: state
      context: "label: the component stores state"
    stateless:
      input: stateless
      context: inline label next to 'state', lowercase, no sentence case
    extensions:
      input: extension
    schema:
      input: schema
      context: inline field label in a technical inspector, lowercase, no sentence case
    input:
      input: input
      context: inline field label in a technical inspector, lowercase, no sentence case
    output:
      input: output
      context: inline field label in a technical inspector, lowercase, no sentence case
    errors:
      input: errors
      context: inline field label in a technical inspector, lowercase, no sentence case
    storage:
      input: storage
      context: inline field label in a technical inspector, lowercase, no sentence case
    associated:
      input: associated
      context: inline field label in a technical inspector, lowercase, no sentence case
    version:
      input: version
      context: inline field label in a technical inspector, lowercase, no sentence case
    binding:
      input: binding
      context: inline field label in a technical inspector, lowercase, no sentence case
    conditioned:
      input: conditioned
      context: inline field label in a technical inspector, lowercase, no sentence case
    adaptive:
      input: adaptive
      context: inline field label in a technical inspector, lowercase, no sentence case
    none:
      input: none
      context: inline field label in a technical inspector, lowercase, no sentence case
  map:
    empty:
      input: The map has no connections yet.
      context: shown when no calls have been observed between components
    back:
      input: Back to the map
      context: button that leaves a focused component and shows the whole map again
    expand:
      input: Show details
      context: accessible name of an icon-only chevron button that opens a component card on the map
