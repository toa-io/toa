namespace: users
name: properties

entity:
  associated: true
  properties:
    newbie:
      type: boolean
  blank:
    newbie: false

operations:
  transit:
    concurrency: retry
    input:
      properties:
        newbie:
          type: string
          default: .
      type: object
      required:
        - newbie
