namespace: interop
name: demo

operations:
  format:
    bridge: '@toa.io/bridges.bash'
    input:
      name*: string
      greeting: Hello
  greet:
    input:
      name*: string
      language*: [en, fr]
