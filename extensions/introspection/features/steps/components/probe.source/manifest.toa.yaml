namespace: probe
name: source

entity:
  associated: true
  storage: null
  properties:
    value:
      type: integer
    secret:
      type: string
  blank:
    value: 0

operations:
  transit:
    concurrency: retry
    input:
      properties:
        value:
          type: string
          default: .
        secret:
          type: string
          default: .
      type: object
  relay:
    input:
      properties:
        a:
          type: number
        b:
          type: number
      type: object
    output:
      type: number
  crash: {}
  fail:
    errors:
      - NOPE
