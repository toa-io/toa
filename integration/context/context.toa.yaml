name: dummies
description: Integration tests context
version: 0.0.0
runtime: .
packages: '*'
registry: localhost:5000

compositions:
  - name: messages
    components:
      - messages.messages
      - stats.stats
