name: echo

operations:
  signal:
    output:
      type: string
  get:
    output:
      type: string
  resolve:
    input:
      type: string
    output:
      type: string

configuration:
  schema:
    type: object
    properties:
      signal:
        type: string
        default: quack

state: ~
