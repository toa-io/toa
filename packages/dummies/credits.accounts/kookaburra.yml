domain: credits

state:
  name: accounts
  schema:
    properties:
      balance:
        type: integer
        default: 0
  inserted: true
operations:
  get:
    template: ~
    http: /{_id}
  update:
    template: ~
    http: /{_id}
  debit:
    schema:
      properties:
        amount:
          type: integer
          minimum: 0
      required: [ amount ]
  credit:
    schema:
      properties:
        amount:
          type: integer
          minimum: 0
      required: [ amount ]
  add: ~
