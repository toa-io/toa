name: converging
namespace: mongo

entity:
  storage: mongodb
  properties:
    foo: &foo
      type: integer
  blank:
    foo: 0

operations:
  transit:
    concurrency: retry
    input:
      properties:
        foo: *foo
      type: object
