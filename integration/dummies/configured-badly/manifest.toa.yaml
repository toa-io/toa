namespace: dummies
name: configured

entity:
  storage: ~
  schema:
    properties:
      foo:
        type: string
        default: 0
      bar:
        type: string
    type: object

configuration:
  type: not-a-type
