entity:
  storage: mongodb
  properties:
    foo:
      type: integer
  blank:
    foo: 0

operations:
  transit:
    concurrency: retry
    input:
      properties:
        inc:
          type: integer
      type: object
