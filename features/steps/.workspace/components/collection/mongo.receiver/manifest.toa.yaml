name: receiver
namespace: mongo

entity:
  associated: true
  properties:
    count:
      type: number
    deleted:
      type: boolean
  blank:
    count: 0
    deleted: false

operations:
  increment:
    concurrency: retry
  delete:
    concurrency: retry

receivers:
  mongo.one.sync: increment
  mongo.one.deleted: delete
