domain: messages
name: messages

entity:
  schema:
    properties:
      text: string

operations:
  - name: add
    input:
      properties:
        text: string
      required: [text]
    output:
      properties:
        id: string
      required: [id]
