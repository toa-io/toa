name: pots
version: 0.0.0

entity:
  schema:
    title: string
    volume: number
    temperature?: number

operations:
  transit:
    query: false
    input:
      title*: ~
      volume*: ~
  observe:
    output:
      id: string
      title: string
      volume: number
      temperature: number
