{
  "name": "toa",
  "version": "0.0.0",
  "private": true,
  "description": "Microservice operations distributed runtime",
  "homepage": "https://toa.io",
  "author": {
    "name": "Artem Gurtovoi",
    "url": "https://github.com/temich"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/toa-io/toa.git"
  },
  "bugs": {
    "url": "https://github.com/toa-io/toa/issues"
  },
  "workspaces": [
    "runtime/*",
    "connectors/*",
    "extensions/*",
    "operations"
  ],
  "engines": {
    "node": ">= 12.0.0"
  },
  "bin": {
    "toa": "runtime/runtime/bin/toa"
  },
  "devDependencies": {
    "@babel/eslint-parser": "7.17.0",
    "@commitlint/cli": "14.1.0",
    "@commitlint/config-conventional": "14.1.0",
    "clone-deep": "4.0.1",
    "execa": "5.1.1",
    "husky": "7.0.4",
    "jest": "27.3.1",
    "node-fetch": "2.6.7",
    "randomstring": "1.2.1",
    "snazzy": "9.0.0",
    "standard": "16.0.4"
  },
  "scripts": {
    "prepare": "npx husky install",
    "test": "npm run test:lint && npm run test:unit",
    "test:lint": "npx standard --verbose | npx snazzy",
    "test:unit": "npx jest --roots runtime connectors extensions",
    "test:integration": "npx jest --roots integration --runInBand",
    "integration:deploy": "npm run publish:local && npx toa deploy ./integration/context",
    "lint": "npx standard --verbose --fix | npx snazzy",
    "publish:local": "npx --yes lerna version --conventional-prerelease --preid dev --force-publish --no-git-tag-version --no-push --no-changelog --yes && npm publish --workspaces",
    "cluster": "chmod +x ./integration/scripts/cluster.sh && ./integration/scripts/cluster.sh",
    "registry:login": "aws ecr get-login-password --region eu-west-1 | docker login --username AWS --password-stdin 153896708942.dkr.ecr.eu-west-1.amazonaws.com",
    "buildx": "docker buildx create --name toa --use && docker buildx inspect --bootstrap",
    "setup": "docker compose up -d && npm run cluster && npm run buildx && npm run publish:local && npm ci && toa deploy ./integration/context",
    "connect": "telepresence quit && telepresence connect && telepresence intercept -l dev",
    "env": "docker compose up -d && npm run cluster && npm adduser --registry http://localhost:4873"
  },
  "standard": {
    "parser": "@babel/eslint-parser",
    "env": {
      "jest": true
    }
  },
  "jest": {
    "roots": [
      "<rootDir>/runtime",
      "<rootDir>/connectors",
      "<rootDir>/extensions",
      "<rootDir>/integration"
    ],
    "testMatch": [
      "**/?(*.)+(test).js"
    ],
    "setupFilesAfterEnv": [
      "<rootDir>/jest.setup.js"
    ],
    "clearMocks": true,
    "bail": true
  }
}
