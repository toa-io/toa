namespace: probe
name: source

entity:
  associated: true
  storage: null
  schema:
    properties:
      value:
        type: integer
        default: 0
      secret:
        type: string
    type: object

operations:
  transit:
    concurrency: retry
    input:
      properties:
        value:
          type: string
          default: .
        secret:
          type: string
          default: .
      type: object
  relay:
    input:
      properties:
        a:
          type: number
        b:
          type: number
      type: object
    output:
      type: number
  crash: {}
  fail:
    errors:
      - NOPE
