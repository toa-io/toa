namespace: custom
name: id

entity:
  schema:
    properties:
      id:
        type: number
      value:
        type: string
        default: hello
    type: object
  associated: true

operations:
  transit:
    concurrency: retry
    input:
      properties:
        value:
          type: string
          default: .
      type: object
