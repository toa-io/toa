domain: dummies
name: origins

entity:
  storage: ~
  schema:
    foo:
      type: string
      default: 0
    bar: string

operations:
  foo:
    concurrency: retry
    input:
      delay: number
    output:
      status: number
      text: string
  bar:
    concurrency: retry
    output:
      status: number
      text: string

origins:
  local: http://localhost:3000
