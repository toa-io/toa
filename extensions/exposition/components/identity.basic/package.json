{
  "name": "identity.basic",
  "version": "0.0.1",
  "dependencies": {
    "bcrypt": "5.1.1"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node"
  },
  "scripts": {
    "install": "npx tsc"
  },
  "devDependencies": {
    "@types/bcrypt": "^5.0.0"
  }
}
