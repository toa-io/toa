node_modules

# Output
# `dist` is ignored by the extension's own .gitignore: npm reads nested ignore files when
# packing, and a rule here would drop the build from the tarball.
/.svelte-kit

# OS
.DS_Store
Thumbs.db

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Playwright
test-results
.features-gen

# App
*.code-workspace
*.temp.*

.env.local
