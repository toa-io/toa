name: echo

operations:
  compute:
    input:
      name*: string
  affect:
    input:
      name*: string
  identity:
    input:
      identity:
        id: string
        roles: [string]
  parameters:
    input: &parameters
      type: object
      properties:
        a: string
        b: string
    output: *parameters
