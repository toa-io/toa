# The version of `contracts.peer` that is already running. `contracts.next` is the same
# component after a change: one more operation, and an event nothing declared before.
name: peer
namespace: contracts

entity:
  storage: ~
  properties:
    foo:
      type: integer
  blank:
    foo: 0

operations:
  serves:
    type: computation
