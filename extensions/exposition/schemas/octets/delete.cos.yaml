properties:
  workflow:
    oneOf:
      - type: object
        patternProperties:
          ^.+$:
            type: string
      - type: array
        items:
          type: object
          patternProperties:
            ^.+$:
              type: string
nullable: true
type: object
