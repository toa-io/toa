namespace: dummies
name: nulls

entity:
  storage: ~
  schema:
    properties:
      foo:
        type: integer
        default: 0
      bar:
        type: string
    type: object

operations:
  transit:
    concurrency: retry

configuration:
  schema:
    type: object
    properties:
      foo:
        type: integer
        default: 1
