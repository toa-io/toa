domain: credits

state:
  name: accounts
  schema:
    properties:
      user: string
      balance:
        type: integer
        default: 0
    required: [ user ]

operations:
  debit:
    schema:
      properties:
        amount:
          type: integer
          minimum: 0
      required: [ amount ]
  credit:
    schema:
      properties:
        amount:
          type: integer
          minimum: 0
      required: [ amount ]
  add: ~
