name: echo

operations:
  signal:
    output: string
  get:
    output: string
  resolve:
    input: string
    output: string

configuration:
  schema:
    type: object
    properties:
      signal:
        type: string
        default: quack

state: ~
