namespace: identity
name: roles

entity:
  schema:
    identity: string
    role: string

operations:
  transit:
    query: false
    input:
      identity*: string
      role*: string
  list:
    output: [string]

exposition:
  /:identity:
    auth:role: system:identity:roles
    POST: transit
    GET: list
