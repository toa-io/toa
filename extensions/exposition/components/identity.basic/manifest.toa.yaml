namespace: identity
name: basic

entity:
  schema:
    username: string
    password: string

operations:
  transit:
    concurrency: retry
    input:
      username: string
      password: string
  resolve:
    input:
      username: string
      password: string
    output:
      id: string

configuration:
  schema:
    rounds: 10
    pepper: ''
