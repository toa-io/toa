namespace: probe
name: source

entity:
  associated: true
  storage: null
  properties:
    value: &value
      type: integer
    secret: &secret
      type: string
  blank:
    value: 0

operations:
  transit:
    concurrency: retry
    input:
      properties:
        value: *value
        secret: *secret
      type: object
  relay:
    input:
      properties:
        a:
          type: number
        b:
          type: number
      type: object
    output:
      type: number
  crash: {}
  fail:
    errors:
      - NOPE
