namespace: bash
name: dummy

bridge: bash

operations:
  echo:
    input:
      properties:
        code:
          type: number
          default: 0
        message:
          type: string
      type: object
