domain: math
name: calculator

operations:
  - name: sum
    schema:
      properties:
        a:
          type: number
        b:
          type: number
      required: [a, b]
  - name: pow
    schema:
      properties:
        a:
          type: number
        b:
          type: number
      required: [a, b]
