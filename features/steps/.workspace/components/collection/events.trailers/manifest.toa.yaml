entity:
  storage: mongodb
  schema:
    properties:
      foo:
        type: integer
        default: 0
    type: object

operations:
  transit:
    concurrency: retry
    input:
      properties:
        inc:
          type: integer
      type: object
