# routes
/^\/(.{1,128}[^\/])?$/: ref:#/

# methods
/^(GET|POST|PUT|PATCH|DELETE)$/:
  endpoint*: string
  type*: [transition, observation, assignment, computation, effect]
  query: object

# directives
/^[a-z0-9]+:[a-z0-9]+$/: { }
