name: streams
namespace: operations

entity:
  properties:
    foo:
      type: number
    bar:
      type: string
  blank:
    foo: 0

operations:
  transit:
    concurrency: retry
    input:
      properties:
        foo:
          type: string
          default: .
        bar:
          type: string
          default: .
      type: object
