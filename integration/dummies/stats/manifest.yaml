domain: stats
name: stats

entity:
  initialized: true
  schema:
    messages:
      type: integer
      default: 0

operations:
  update:
    concurrency: retry
    input:
      messages: boolean

receivers:
  messages.messages.created: update

resources:
  /:id:
    operations: [observe, update]
