namespace: identity
name: bans
version: 0.0.0

entity:
  schema:
    banned: false
  dependent: true

operations:
  assign:
    input:
      banned: true

exposition:
  isolated: true
  /:id:
    auth:role: system:identity:bans
    PUT: assign
