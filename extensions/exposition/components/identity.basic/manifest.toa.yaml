namespace: identity
name: basic

entity:
  schema:
    username: string
    password: string

operations:
  transit:
    concurrency: retry
    input:
      username: string
      password: string
  resolve:
    input: string
    output:
      id: string

configuration:
  rounds: 10
  pepper: ''

exposition:
  /:
    POST: transit
  /:id:
    PUT: transit
