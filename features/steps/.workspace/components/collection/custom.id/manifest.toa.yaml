namespace: custom
name: id

entity:
  associated: true
  properties:
    id:
      type: number
    value:
      type: string
  blank:
    value: hello

operations:
  transit:
    concurrency: retry
    input:
      properties:
        value:
          type: string
          default: .
      type: object
