name: one
namespace: mongo

entity:
  storage: mongodb
  properties:
    foo:
      type: integer
    bar:
      type: string
    baz:
      type: boolean
  blank:
    foo: 0

operations:
  transit:
    concurrency: retry
    input:
      properties:
        foo:
          type: string
          default: .
        bar:
          type: string
          default: .
        baz:
          type: string
          default: .
      type: object
  assign:
    input:
      properties:
        foo:
          type: string
          default: .
        bar:
          type: string
          default: .
        baz:
          type: string
          default: .
      type: object
  nothing:
    concurrency: retry
