name: storage

storages: tmp

operations:
  put:
    input:
      properties:
        storage:
          type: string
        path:
          type: string
      type: object
      required:
        - storage
        - path
  get:
    input:
      properties:
        storage:
          type: string
        path:
          type: string
      type: object
      required:
        - storage
        - path
