name: echo

operations:
  compute:
    input:
      name*: string
  affect:
    input:
      name*: string
  identity:
    input:
      identity:
        id: string
        roles: [string]
  parameters:
    input: &parameters
      type: object
      properties:
        a:
          type: string
        b:
          type: string
    output: *parameters
  echo:
    input:
      type: object
  unwrap:
    input:
      type: object
      properties:
        buf: string
