name: one
namespace: mongo

entity:
  storage: mongodb
  schema:
    properties:
      foo:
        type: integer
        default: 0
      bar:
        type: string
      baz:
        type: boolean
    type: object

operations:
  transit:
    concurrency: retry
    input:
      properties:
        foo:
          type: string
          default: .
        bar:
          type: string
          default: .
        baz:
          type: string
          default: .
      type: object
  assign:
    input:
      properties:
        foo:
          type: string
          default: .
        bar:
          type: string
          default: .
        baz:
          type: string
          default: .
      type: object
  nothing:
    concurrency: retry
