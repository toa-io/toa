namespace: configuration
name: base

operations:
  echo:
    output:
      foo: string
      bar: string
      num: integer

configuration:
  schema:
    type: object
    properties:
      foo:
        type: string
        default: hello
      bar:
        type: string
        default: world
      num:
        type: integer
        default: 0
