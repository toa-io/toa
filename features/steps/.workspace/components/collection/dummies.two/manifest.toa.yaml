name: two
namespace: dummies
version: 0.0.1

entity:
  storage: ~
  schema:
    foo:
      type: integer
      default: 0
    bar: string

operations:
  transit:
    concurrency: retry
    input:
      foo: .
      bar: .
