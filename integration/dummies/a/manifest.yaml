domain: dummies
name: a

entity:
  schema:
    properties:
      title: string
      length: integer
    required: [title]
  initialized: true

operations:
  transit:
    input:
      title: ~
  create:
    forward: transit
    query: false
    input:
      properties:
        title: ~
        length: ~

receivers:
  messages.messages.created: transit

extensions:
  resources:
    /:
      operations: [create]
    /:id:
      operations: [observe]
