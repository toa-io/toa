prototype: ../a

namespace: dummies
name: b

entity:
  schema:
    properties:
      length:
        type: integer
        maximum: 10
      description:
        type: string
    required:
      - length
    type: object

receivers:
  dummies.a.happened: transit
