name: users

entity:
  properties:
    name:
      type: string

operations:
  transit:
    concurrency: retry
    input:
      properties:
        name:
          type: string
          default: .
      type: object
      required:
        - name
  create:
    concurrency: retry
    query: false
    errors: [0]
    input:
      properties:
        name:
          type: string
          default: .
      type: object
      required:
        - name
