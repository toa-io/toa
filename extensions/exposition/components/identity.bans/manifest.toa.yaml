namespace: identity
name: bans

entity:
  schema:
    banned: false
  dependent: true

operations:
  assign:
    input:
      banned: true

exposition:
  isolated: true
  /:id:
    io:output: false
    auth:role: system:identity:bans
    PUT: assign
