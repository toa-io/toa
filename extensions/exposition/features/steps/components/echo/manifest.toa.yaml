name: echo

operations:
  compute:
    input:
      properties:
        name:
          type: string
      type: object
      required:
        - name
  affect:
    input:
      properties:
        name:
          type: string
      type: object
      required:
        - name
  identity:
    input:
      properties:
        identity:
          properties:
            id:
              type: string
            roles:
              type: array
              items:
                type: string
          type: object
      type: object
  parameters:
    description: Answers with what it was given.
    input:
      type: object
      properties:
        a:
          type: string
        b:
          type: string
    output:
      type: object
      properties:
        a:
          type: string
        b:
          type: string
  echo:
    input:
      type: object
  unwrap:
    input:
      type: object
      properties:
        buf:
          type: string
  error:
    errors: [CODE]
