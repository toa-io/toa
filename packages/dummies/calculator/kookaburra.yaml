domain: math
name: calculator

operations:
  - name: sum
    schema:
      properties:
        a:
          type: number
        b:
          type: number
      required: [a, b]
    http:
      - path: /sums
  - name: multi
    schema:
      properties:
        a:
          type: number
        b:
          type: number
      required: [a, b]
    http:
      path: /multiplications
