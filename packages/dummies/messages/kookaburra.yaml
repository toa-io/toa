domain: messages
name: messages

entity:
  schema:
    properties:
      text: string

operations:
  - name: add
    input:
      type: object # TODO
      properties:
        text:
          type: string
      required: [text]
