prototype: null

entity:
  properties:
    id:
      type: string
      pattern: ^[a-fA-F0-9]{32}$
    VERSION:
      type: integer
      minimum: 0
    CREATED:
      type: integer
      format: epoch-millis
    UPDATED:
      type: integer
      format: epoch-millis
    DELETED:
      type: integer
      format: epoch-millis
      nullable: true
  required:
    - id

operations:
  transit:
    virtual: true
