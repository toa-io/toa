namespace: probe
name: quiet

# opted out entirely: neither described nor instrumented
introspection: false

operations:
  ping:
    input:
      properties:
        a:
          type: number
      type: object
    output:
      type: number
