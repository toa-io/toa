namespace: users
name: properties

entity:
  schema:
    properties:
      newbie:
        type: boolean
        default: false
    type: object
  associated: true

operations:
  transit:
    concurrency: retry
    input:
      properties:
        newbie:
          type: string
          default: .
      type: object
      required:
        - newbie
