namespace: mongo
name: associated

entity:
  associated: true
  schema:
    properties:
      foo:
        type: integer
        default: 0
      bar:
        type: string
    type: object

operations:
  transit:
    concurrency: retry
    input:
      properties:
        foo:
          type: integer
          default: 0
        bar:
          type: string
      type: object
  undelete:
    concurrency: retry
    input:
      properties:
        foo:
          type: integer
          default: 0
        bar:
          type: string
      type: object
  increment:
    concurrency: retry
    input:
      properties:
        foo:
          type: integer
          default: 0
      type: object
