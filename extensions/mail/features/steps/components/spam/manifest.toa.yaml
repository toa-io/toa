name: spam

operations:
  send:
    input:
      type: object
      properties:
        to: { type: string }
        subject: { type: string }
        text: { type: string }
        template: { type: string }
        data: { type: object }
      required: [to]
      oneOf:
        - required: [subject, text]
        - required: [template]

mail: ~
