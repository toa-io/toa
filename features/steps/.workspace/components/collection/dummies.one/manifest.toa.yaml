name: one
namespace: dummies

entity:
  storage: ~
  properties:
    foo:
      type: integer
    bar:
      type: string
  blank:
    foo: 0

operations:
  transit:
    concurrency: retry
    input:
      properties:
        foo:
          type: string
          default: .
        bar:
          type: string
          default: .
      type: object
