name: spam

operations:
  send:
    input:
      properties:
        from: { type: string }
        to: { type: string }
        subject: { type: string }
        text: { type: string }
      required: [from, to, subject, text]

mail: ~
