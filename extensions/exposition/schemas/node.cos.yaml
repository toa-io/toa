# routes
/^\/(.{1,128}[^\/])?$/: ref:#/

# methods
/^(GET|POST|PUT|PATCH|DELETE)$/:
  namespace: default
  component*: string
  endpoint*: string
  type*: [transition, observation, assignment, computation, effect]
  query:
    $ref: query
    default: { }

# directives
/^[a-z0-9]+:[a-z0-9]+$/: { }
