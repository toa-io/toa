name: one
namespace: dummies

entity:
  storage: ~
  properties:
    foo: &foo
      type: integer
    bar: &bar
      type: string
  blank:
    foo: 0

operations:
  transit:
    concurrency: retry
    input:
      properties:
        foo: *foo
        bar: *bar
      type: object
