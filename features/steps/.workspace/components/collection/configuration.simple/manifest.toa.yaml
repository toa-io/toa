namespace: configuration
name: simple

entity:
  storage: ~
  schema:
    foo:
      type: string
      default: 0
    bar: string

operations:
  transit:
    concurrency: retry
    output:
      id: .
      foo: string

configuration:
  foo: Hello
  bar:
    a: 1
    b: 2
