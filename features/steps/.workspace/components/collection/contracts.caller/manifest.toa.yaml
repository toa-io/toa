# Receives an event that only `contracts.next` declares, and calls an operation that only
# `contracts.next` provides.
name: caller
namespace: contracts

entity:
  storage: ~
  properties:
    count:
      type: integer
  blank:
    count: 0

operations:
  noted:
    concurrency: retry

receivers:
  contracts.peer.appeared: noted
