namespace: dummies
name: associated

entity:
  associated: true
  schema:
    properties:
      a:
        type: integer
        default: 10
      b: string
    required: [a, b]

operations:
  assign:
    input:
      a: .
      b: .
