definitions:
  component:
    anyOf:
      - type: string
        enum:
          - ip
          - path
          - route
          - identity
      - type: object
        properties:
          segment:
            type: string
        required:
          - segment
        additionalProperties: false
  condition:
    type: object
    properties:
      status:
        type: number
type: object
properties:
  key:
    anyOf:
      - anyOf:
          - type: string
            enum:
              - ip
              - path
              - route
              - identity
          - type: object
            properties:
              segment:
                type: string
            required:
              - segment
            additionalProperties: false
      - type: array
        items:
          anyOf:
            - type: string
              enum:
                - ip
                - path
                - route
                - identity
            - type: object
              properties:
                segment:
                  type: string
              required:
                - segment
              additionalProperties: false
        minItems: 1
  condition:
    anyOf:
      - type: object
        properties:
          status:
            type: number
      - type: array
        items:
          type: object
          properties:
            status:
              type: number
        minItems: 1
  requests:
    type: integer
    minimum: 1
  interval:
    description: Seconds
    type: integer
    minimum: 1
additionalProperties: false
required:
  - key
  - requests
  - interval
