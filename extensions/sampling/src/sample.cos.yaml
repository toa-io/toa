reply: &reply
  output: ~
  error: ~
context:
  local: &calls
    ~:
      - request:
          input: ~
          query: string
        reply: *reply
  remote: *calls
storage:
  current+: object
  next: object
extensions:
  ~:
    - arguments: [~]
      result: ~
      permanent: boolean
