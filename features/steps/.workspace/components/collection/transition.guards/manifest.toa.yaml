name: guards
namespace: transition

entity:
  storage: ~
  properties:
    a:
      type: integer
    b:
      type: integer
  required:
    - a
    - b

operations:
  transit:
    concurrency: retry
    input:
      type: object
      properties:
        a:
          type: integer
        b:
          type: integer
      required:
        - a
        - b
