# The values component is TypeScript and its operations are transpiled into this
# directory, so it is generated.
components/configuration.values/operations/

# the UI build. Kept here rather than in `ui/.gitignore` because npm reads nested ignore
# files when packing, and a rule there would drop it from the tarball.
/ui/dist/
