namespace: probe
name: target

entity:
  storage: null
  schema:
    properties:
      counted:
        type: integer
        default: 0
    type: object

operations:
  compute:
    input:
      properties:
        a:
          type: number
        b:
          type: number
      type: object
    output:
      type: number
  count:
    concurrency: retry
    input:
      type: object
      additionalProperties: true

receivers:
  probe.source.created: count
