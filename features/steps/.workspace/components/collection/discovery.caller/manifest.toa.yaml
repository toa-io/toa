# Receives an event that only `discovery.peer.next` declares, and calls an operation that only
# `discovery.peer.next` provides.
name: caller
namespace: discovery

entity:
  storage: ~
  properties:
    count:
      type: integer
  blank:
    count: 0

operations:
  noted:
    concurrency: retry

receivers:
  discovery.peer.appeared: noted
