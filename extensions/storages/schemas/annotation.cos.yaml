type: object
patternProperties:
  ^[a-z0-9_]{1,32}$:
    oneOf:
      - $ref: s3
      - $ref: spaces
      - $ref: cloudinary
      - $ref: fs
      - $ref: tmp
      - $ref: test
additionalProperties: false
