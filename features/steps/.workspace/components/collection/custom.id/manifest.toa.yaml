namespace: custom
name: id

entity:
  associated: true
  properties:
    id:
      type: number
    value: &value
      type: string
  blank:
    value: hello

operations:
  transit:
    concurrency: retry
    input:
      properties:
        value: *value
      type: object
