namespace: probe
name: discreet

# on the map, but its payloads are never captured
introspection:
  samples: false

operations:
  ping:
    input:
      properties:
        a:
          type: number
      type: object
    output:
      type: number
