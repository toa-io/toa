domain: credits
name: balance

entity:
  schema:
    properties:
      balance:
        type: integer
        default: 10
  initialized: true

operations:
  deduce:
    input: integer
    output: integer
    query: true
  transfer:
    input: id   # source balance
    query: true # target balance
  nullify:
    output: integer
