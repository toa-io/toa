name: pots

entity:
  schema:
    title: string
    volume: number

operations:
  transit:
    concurrency: none
    input:
      title: ~
      volume: ~

exposition:
  /:
    POST: transit
    GET: observe
