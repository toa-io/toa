name: notify

operations:
  send:
    input:
      properties:
        id:
          type: string
        title:
          type: string
        volume:
          type: number
        price:
          type: number
      type: object
    output:
      {}

receivers:
  orders.created: send
