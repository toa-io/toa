namespace: dummies
name: nulls

entity:
  storage: ~
  schema:
    foo:
      type: integer
      default: 0
    bar: string

operations:
  transit:
    concurrency: retry

configuration:
  schema:
    type: object
    properties:
      foo:
        type: string
        default: bar
