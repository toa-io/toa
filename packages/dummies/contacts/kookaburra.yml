domain: contacts

state:
  schema:
    properties:
      user: string
      contact: string
      label: string
    required: [ user, contact ]
  inserted: true

operations:
  find:
    template: ~
    http: /{user}
  update:
    template: ~
    schema:
      properties:
        label: ~
