name: two
namespace: sql

entity:
  storage: sql
  schema:
    properties:
      foo:
        type: integer
        default: 0
      bar:
        type: string
    type: object

operations:
  transit:
    concurrency: retry
    input:
      properties:
        foo:
          type: string
          default: .
        bar:
          type: string
          default: .
      type: object
