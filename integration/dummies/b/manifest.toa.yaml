prototype: ../a

namespace: dummies
name: b

entity:
  schema:
    properties:
      length:
        maximum: 10
      description: string
    required: [length]

receivers:
  dummies.a.happened: transit
