namespace: configuration
name: base

operations:
  echo:
    output:
      properties:
        foo:
          type: string
        bar:
          type: string
        num:
          type: integer
      type: object

configuration:
  schema:
    type: object
    properties:
      foo:
        type: string
        default: hello
      bar:
        type: string
        default: world
      num:
        type: integer
        default: 0
