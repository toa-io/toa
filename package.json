{
  "name": "@toa.io/toa",
  "version": "0.3.0",
  "private": true,
  "description": "Microservice operations distributed runtime",
  "homepage": "https://toa.io",
  "author": {
    "name": "Artem Gurtovoi",
    "url": "https://github.com/temich"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/toa-io/toa.git"
  },
  "bugs": {
    "url": "https://github.com/toa-io/toa/issues"
  },
  "workspaces": [
    "runtime/*",
    "connectors/*",
    "extensions/*",
    "libraries/*",
    "operations",
    "userland",
    "types"
  ],
  "engines": {
    "node": ">= 12.0.0"
  },
  "bin": {
    "toa": "runtime/runtime/bin/toa"
  },
  "peerDependencies": {
    "typescript": "^4.9.5"
  },
  "devDependencies": {
    "@commitlint/cli": "14.1.0",
    "@commitlint/config-conventional": "14.1.0",
    "@cucumber/cucumber": "9.1.2",
    "@types/jest": "29.5.3",
    "@types/node": "20.3.1",
    "@types/randomstring": "1.1.8",
    "clone-deep": "4.0.1",
    "cucumber-tsflow": "4.0.6",
    "dotenv": "16.0.3",
    "eslint-config-standard-with-typescript": "38.0.0",
    "execa": "5.1.1",
    "husky": "8.0.3",
    "jest": "29.5.0",
    "jest-diff": "29.6.2",
    "knex": "2.4.2",
    "mongodb": "5.7.0",
    "node-fetch": "2.6.11",
    "nopable": "0.1.0",
    "randomstring": "1.3.0",
    "ts-jest": "29.1.0",
    "ts-node": "10.9.1"
  },
  "scripts": {
    "prepare": "npx husky install",
    "test": "npm run test:lint && npm run test:unit",
    "test:lint": "npx eslint .",
    "test:unit": "npx jest --roots runtime connectors extensions operations libraries features userland",
    "test:stage": "npx jest --roots userland/example/stage --testPathIgnorePatterns .*/transpiled/",
    "test:integration": "npx jest --roots integration --runInBand --testTimeout 180000",
    "test:features": "npx cucumber-js && npm run test:features --workspaces --if-present",
    "test:samples:unit": "toa replay userland/example/components/*",
    "test:samples:all": "TOA_DEV=1 toa replay userland/example",
    "test:all": "npm run ci && npm run test && npm run test:integration && npm run test:stage && npm run test:features",
    "lint": "npx eslint . --fix",
    "npm:login:local": "npm adduser --registry http://localhost:4873 --auth-type=legacy",
    "version:local": "npx lerna@6.5.1 version --conventional-prerelease --preid local --no-commit-hooks --no-changelog --yes --git-tag-command 'echo %s'",
    "publish:local": "npm publish from-package -ws --registry=http://localhost:4873",
    "cluster": "chmod +x ./integration/scripts/cluster.sh && ./integration/scripts/cluster.sh",
    "buildx": "docker buildx create --name toa --use && docker buildx inspect --bootstrap",
    "setup": "docker compose up -d && npm run cluster && npm run buildx && npm run publish:local && npm ci && toa deploy ./integration/context",
    "ci": "rm -rf node_modules && npm exec --workspaces -c \"rm -rf node_modules\" && npm i",
    "transpile": "npm exec --workspaces -c \"rm -rf transpiled\" && npx lerna@6.5.1 run transpile"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "roots": [
      "<rootDir>/runtime",
      "<rootDir>/connectors",
      "<rootDir>/extensions",
      "<rootDir>/operations",
      "<rootDir>/libraries",
      "<rootDir>/integration",
      "<rootDir>/features",
      "<rootDir>/userland"
    ],
    "testMatch": [
      "**/(*.)+(test).(js|ts)"
    ],
    "testPathIgnorePatterns": [
      ".*/transpiled/",
      "<rootDir>/userland/example/stage"
    ],
    "bail": true
  }
}
