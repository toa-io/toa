name: pots
namespace: tea

entity:
  storage: mongodb
  schema:
    material*: [glass, ceramic, steel]
    volume: number

operations:
  transit:
    concurrency: none
    input:
      material*: .
      volume: .
  same:
    input: .material
    output: array

configuration:
  step: 0.5
