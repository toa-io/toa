domain: stats
name: stats

entity:
  initialized: true
  schema:
    properties:
      messages:
        type: integer
        default: 0

operations:
  update:
    input:
      properties:
        messages: boolean

receivers:
  messages.messages.created: update
