name: streams
namespace: operations

entity:
  schema:
    properties:
      foo:
        type: number
        default: 0
      bar:
        type: string
    type: object

operations:
  transit:
    concurrency: retry
    input:
      properties:
        foo:
          type: string
          default: .
        bar:
          type: string
          default: .
      type: object
