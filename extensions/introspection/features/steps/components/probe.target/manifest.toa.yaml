namespace: probe
name: target

entity:
  storage: null
  properties:
    counted:
      type: integer
  blank:
    counted: 0

operations:
  compute:
    input:
      properties:
        a:
          type: number
        b:
          type: number
      type: object
    output:
      type: number
  count:
    concurrency: retry
    input:
      type: object
      additionalProperties: true

receivers:
  probe.source.created: count
