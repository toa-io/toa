name: counter

operations:
  whoami:
    type: computation
    output:
      type: object
      properties:
        instance:
          type: string
  whereami:
    type: computation
    stateful: true
    output:
      type: object
      properties:
        instance:
          type: string
