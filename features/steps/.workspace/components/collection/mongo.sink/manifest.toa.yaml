name: sink
namespace: mongo

entity:
  associated: true
  schema:
    count: 0
    inc: 0

operations:
  increment:
    concurrency: retry
  record:
    concurrency: retry
    input:
      inc: integer

receivers:
  mongo.outbox.sync: increment
  mongo.outbox.incremented: record
