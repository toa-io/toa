prototype: "../configuration.base"
namespace: configuration
name: extended

configuration:
  schema:
    type: object
    properties:
      baz:
        type: string
        default: something
      qux:
        type: integer
        default: 1
