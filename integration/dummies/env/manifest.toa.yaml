namespace: dummies
name: env

entity:
  schema:
    properties:
      length:
        properties:
          maximum:
            type: number
            default: 10
        type: object
      description:
        type: string
    required:
      - length
    type: object
