name: one
namespace: exposed

entity:
  storage: ~
  properties:
    foo: &foo
      type: number
    bar: &bar
      type: string
  blank:
    foo: 0

operations:
  transit:
    concurrency: retry
    input:
      properties:
        foo: *foo
        bar: *bar
      type: object

exposition:
  /:
    POST: transit
