properties:
  verb:
    type: string
    pattern: ^(GET|HEAD|POST|PUT|PATCH|DELETE|LOCK|UNLOCK)$
  mapping:
    properties:
      namespace:
        type: string
      component:
        type: string
      endpoint:
        type: string
      query:
        $ref: query
    additionalProperties: false
    type: object
  directives:
    type: array
    items:
      $ref: directive
type: object
