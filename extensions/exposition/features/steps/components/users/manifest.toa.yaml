name: users

entity:
  schema:
    properties:
      name:
        type: string
    type: object

operations:
  transit:
    concurrency: retry
    input:
      properties:
        name:
          type: string
          default: .
      type: object
      required:
        - name
  create:
    concurrency: retry
    query: false
    errors: [0]
    input:
      properties:
        name:
          type: string
          default: .
      type: object
      required:
        - name
