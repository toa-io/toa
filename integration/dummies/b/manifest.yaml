prototype: ../a

domain: dummies
name: b

entity:
  schema:
    properties:
      length:
        maximum: 10
      description: string
    required: [ length ]

remotes: [ 'a.b', 'd.e' ]

receivers:
  dummies.a.happened: transit
