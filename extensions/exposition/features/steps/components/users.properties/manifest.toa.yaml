namespace: users
name: properties

entity:
  associated: true
  properties:
    newbie: &newbie
      type: boolean
  blank:
    newbie: false

operations:
  transit:
    concurrency: retry
    input:
      properties:
        newbie: *newbie
      type: object
      required:
        - newbie
