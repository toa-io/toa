properties:
  query:
    properties:
      id:
        type: string
        pattern: ^[a-fA-F0-9]+$
      criteria:
        type: string
      sort:
        type: string
        pattern: ''
      omit:
        type: integer
      limit:
        type: integer
      projection:
        type: string
        pattern: ''
    additionalProperties: false
additionalProperties: false
