# Migrating from 1.0.0-alpha.285

The system properties of an entity are renamed, indexes and data changes are declared as
migrations, an entity declares properties rather than a schema, and the SQL storage connector is
gone. A database written by an earlier release is not readable by this one.

**This release requires downtime**: the two versions must not serve at once. See
[The old and the new version must not run at once](#the-old-and-the-new-version-must-not-run-at-once).

## The names

| before     | after     |
| ---------- | --------- |
| `_version` | `VERSION` |
| `_created` | `CREATED` |
| `_updated` | `UPDATED` |
| `_deleted` | `DELETED` |

They are uppercase so that a system property is told apart from one of yours at a glance, wherever
a record is read — in an algorithm, in a criteria string, in a reply a client receives. `id` is
unchanged.

```javascript
export function transition(input, object) {
  return { created: object._created, version: object._version }
}
```

```javascript
export function transition(input, object) {
  return { created: object.CREATED, version: object.VERSION }
}
```

The new names are ordinary property names, so a manifest that still declares an old one is refused
where any misspelled property is:

```
/entity/schema/properties: pattern must match pattern "^[a-zA-Z]+([_a-zA-Z0-9]*[a-zA-Z0-9]+)?$"
```

## Where they are written

Everything that names a property of a record:

```yaml
entity:
  schema:
    properties:
      _created: { type: integer }

exposition:
  /:
    GET:
      io:output: [id, title, _created]
      query:
        sort: _created:desc
```

```yaml
entity:
  schema:
    properties:
      CREATED: { type: integer }

exposition:
  /:
    GET:
      io:output: [id, title, CREATED]
      query:
        sort: CREATED:desc
```

A criteria, a sort and a projection name properties, so they carry the new names too — in a
manifest, and in the query string a client sends:

```http
GET /pots/?criteria=_deleted=isnull=true&sort=_created:desc HTTP/1.1
```

```http
GET /pots/?criteria=DELETED=isnull=true&sort=CREATED:desc HTTP/1.1
```

A realtime route names the payload property it keys a stream by, and an event's payload is the
record, so a route keyed by one of these names it the new way.

## Existing data

The four are stored under their new names, so a record written by an earlier release is missing
every one of them as far as this one is concerned: it reads as never created, never updated, never
deleted and at version zero. Every index over them is over names nothing writes any more.

The runtime renames them at start, once per collection, before the component serves — and, from
the release after this one, converts the timestamps to dates at the same start; see
[286](286.md#a-moment-is-stored-as-a-date). Nothing is declared for it. What is required is that
every replica of this release is stopped first, see below.

An index over one of the four is over a name nothing writes any more. Declare it again, over the
new name, in a [migration](/documentation/component/declaration.md#migrations) — see
[What else is rebuilt](#what-else-is-rebuilt).

## A page built on `svas`

`svas` merges a record by `_version` and removes it on `_deleted`. A page that uses `collection`,
`value` or `merge` needs the release of `svas` that reads the new names.

## The old and the new version must not run at once

**This release is not deployed by a rolling update.** Take the whole deployment down, then bring
this one up.

The first replica of this release to start renames the properties across the collection, and from
that moment a replica of the release before it is reading a database it does not understand. Two things happen to it, and the first is silent:

- **Every deleted record reads as live.** It filters `_deleted: null`, and a missing field matches
  that, so every tombstone comes back — a deleted identity is a live one again, a revoked role is
  granted again.
- **Every transition fails.** Its compare-and-swap is on `_version`, which no record carries any
  more, so nothing matches, the operation retries and throws.

A rolling update runs both versions on purpose, which is exactly what must not happen here. Scale
the deployment to zero, or delete it, and only then apply this one. The gap is the migration:
seconds on a small collection, longer on a large one, and no replica serves until it is done.

Rolling back has the same shape in reverse: a database this release has migrated cannot be read
by the release before it. Going back means stopping everything and restoring the backup
taken before the upgrade, which is what a rollback is built on.

## A reply to an unsafe request carries no `etag`

The gateway tags a reply with its `VERSION` on `GET` and `HEAD` only. A `POST` or a `PUT` that
used to answer `etag: "1"` answers no tag: the version of what it wrote is what the next `GET`
carries, or what the operation puts in its reply. A reply that carries no `VERSION` is not tagged
at all: the hash of the body that stood in for a version is gone, along with the `304` it
answered.

```http
POST /pots/ HTTP/1.1

---

HTTP/1.1 201 Created
etag: "1"

id: ...
```

```http
POST /pots/ HTTP/1.1

---

HTTP/1.1 201 Created

id: ...
```

`if-match` is read on any method as before. `if-none-match` is answered on `GET` and `HEAD`, by
the version.

## `server-timing` says one thing

The header carries the time from the request received to the reply written, as `total`, and
nothing per stage.

## Indexes move into migrations

`entity.index` and `entity.unique` are no longer part of a manifest, and one that still declares
them fails validation:

```
Property index is not expected to be here
```

What they declared is now a file in the component's `migrations` directory. Write one that
reproduces the indexes the database already has, and delete the manifest block:

```yaml
# manifest.toa.yaml — before
entity:
  unique:
    authority_username: [authority, username]
  index:
    username:
      authority: asc
      username: asc
```

```yaml
# migrations/0001-indexes.yaml — after
- index:
    name: unique_authority_username
    keys: { authority: asc, username: asc }
    unique: true
- index:
    name: index_username
    keys: { authority: asc, username: asc }
```

The translation is mechanical, and it has to be exact, or the first run drops a live index and
builds it again:

- `unique: { <key>: [<fields>] }` becomes `name: unique_<key>`, `keys` with every field `asc`,
  `unique: true`, and `partial: { DELETED: null }` — see below. Where a field is not in
  `schema.required`, add `<field>: { $exists: true }` to that same `partial`.
- `index: { <key>: { <field>: <direction> } }` becomes `name: index_<key>` and the same `keys`.
  Where a field is not in `schema.required`, add `sparse: true`.

`VERSION`, `CREATED`, `UPDATED` and `DELETED` count for that rule like any other field: a
component that does not list one in `required` had a sparse index over it, and the migration says
`sparse: true` to keep it.

Written that way an ordinary index is a no-op against a database that already has it. A unique
one is rebuilt, because of the filter the next section adds. Get the rest wrong and the first run
drops a live index and builds it again, which on a large collection is a stall, not a failure.

### A unique index holds over live records only

Nothing is hard-deleted, so a tombstone under a plain unique index holds its key for good: delete
an identity and its username can never be registered again, revoke a role and it can never be
granted again. Every unique index this release ships is declared with a filter that leaves
tombstones out, and yours should be:

```yaml
- index:
    name: unique_email
    keys: { email: asc }
    unique: true
    partial: { DELETED: null }
```

Uniqueness still holds among live records; only the tombstones stop reserving keys. The index is
rebuilt on the first run, because the live one has no such filter.

### What else is rebuilt

One index gets rebuilt whatever you write: one whose keys include `_version`, `_created`,
`_updated` or `_deleted`. The live index is over a name nothing writes after the rename of this
same release, so it is dropped and built again over the new one. Three of the components shipped
here are in that position — `configuration.values`, `identity.federation` and `identity.passkeys`
— and so is any of yours that sorts or filters on one of the four.

See [Migrations](/documentation/component/declaration.md#migrations) for what a migration may say.

### An index changes by being edited

Changing an index used to require renaming it. Under its old name the change was refused by
MongoDB, the refusal was logged as a warning, and the database kept the index it had.

Now an index whose name is held by one of another shape is dropped and made again, so an index
changes by editing its declaration — in a new migration, because a migration that has run is not
run again:

```yaml
# migrations/0007-widen-the-lookup.yaml
- index:
    name: index_lookup
    keys: { component: asc, epoch: asc, CREATED: desc }
```

### Migrations are inherited

A prototype's migrations are applied to the collection of every component that takes it, ahead of
the component's own, and recorded as `<prototype>:<id>`. A prototype that declares migrations has
a `name`. The runtime's own — the rename of this release, the dates of the next — are the root
prototype's, recorded as `system:<id>`, and a component declares nothing for them.

### What is applied, and when

A migration is applied once for the database rather than once per replica, and never again. The
runtime keeps a `system_migrations` collection beside the component collections to record that;
dropping a row is what makes a migration run again.

A migration runs before the component serves and the other replicas wait for it, so a rollout
waits for the longest one. A replica that dies while applying a migration has it applied again
from its first step, which is why a migration must be idempotent — write an `update` filter that
excludes what it has already changed.

Nothing prunes an index any more. Before, an index the manifest did not name was dropped on every
start; now an index nobody declared is left alone, and one that is no longer wanted is dropped by
a `dropIndex` step.

### Deployment procedure

**This release is not deployed by a rolling update**, because of the system-property rename it
ships with — see [The old and the new version must not run at once](#the-old-and-the-new-version-must-not-run-at-once). Stop every
replica of the release before it first.

That is a property of this release, not of migrations. An index migration is safe to roll out
either way; a data migration needs the release before it to still be able to read what it leaves,
and where it cannot, the deployment stops first.

1. Write `migrations/0001-indexes.yaml` for every stored component that declares `index` or
   `unique`, and remove the declarations.
2. Confirm the manifests still validate:

   ```shell
   toa export deployment <environment>
   ```

3. Back the database up, then stop every replica of the release before this one.
4. Deploy. Read `system_migrations` afterwards to see what was applied:

   ```
   { _id: 'identity_basic:0002-indexes', state: 'done', ... }
   ```

   A row still `running` long after a rollout is a migration whose replica died and whose lease
   has not yet been taken over; a row that is `done` needs nothing.

### Rollback

An older runtime ignores `migrations` and manages `entity.index` again — but the manifests no
longer declare any, so it would find none and drop every index the migration built. Roll the
Context back together with the runtime, or not at all.

It cannot read the records either, for the same reason the two versions must not run at once, so
a rollback is a stop, a restore of the backup taken before the upgrade, and a start — not a
redeploy of the version before this one over the database it left behind.

## An entity declares properties, not a schema

`entity.schema` is gone. What was inside it is declared beside `storage`: the property map as
`entity.properties`, and the list of the properties a whole record has as `entity.required`.

```yaml
# before
entity:
  schema:
    type: object
    properties:
      material: { type: string }
      volume: { type: number }
      booked: { type: boolean, default: false }
    required:
      - material
```

```yaml
# after
entity:
  properties:
    material: { type: string }
    volume: { type: number }
    booked: { type: boolean }
  required:
    - material
  blank:
    booked: false
```

The runtime assembles the schema a stored record must fit from the two, and a second one for a
changeset — the same properties, none of them required. So the envelope is no longer yours to
write, and nothing else that stood at the schema's top level survives: a `type`, an
`additionalProperties`, a `definitions` map. A manifest that still declares one is refused:

```
/entity Property additionalProperties is not expected to be here
```

`required` and `blank` may only name a property the entity declares, and a component that names
one it does not fails to load:

```
Entity requires property 'meterial', which is not defined
```

## What a record starts as is declared as `blank`

A `default` in an entity property used to be written into a new record while it was validated.
Validation no longer writes anything, so what a record starts as is declared for what it is:

```yaml
entity:
  properties:
    booked: { type: boolean }
  blank:
    booked: false
```

A blank need not be a whole record — `required` is not enforced against it — but each value must
fit the property it names, and it is fitted when the component boots rather than when the first
record is written:

```
Component 'tea.pots' entity blank: booked must be boolean
```

`id`, `VERSION`, `CREATED`, `UPDATED` and `DELETED` are the runtime's to write, so a blank may not
name one, and neither may a `default` stand in for one any more. A manifest that declared
`VERSION: { default: 0 }` or `DELETED: { default: null }` drops it: the runtime writes both.

## A schema's `default` fills nothing

Validating a value no longer writes to it, so a `default` written into a schema is an annotation
and nothing more. It stops working in three places.

**An entity property.** Declare it as [`blank`](#what-a-record-starts-as-is-declared-as-blank).

**An operation's `input`.** A property a caller omits stays omitted, whatever the schema says. A
value an operation cannot run without is `required`, and one it can is read as absent:

```yaml
# before
operations:
  increment:
    input:
      properties:
        value: { type: number }
        times: { type: number, default: 1 }
```

```yaml
# after
operations:
  increment:
    input:
      properties:
        value: { type: number }
        times: { type: number }
      required: [value, times]
```

**A `configuration` schema.** What a component is configured with by default is `defaults`, beside
the schema, which is where it belonged already:

```yaml
# before
configuration:
  schema:
    type: object
    properties:
      lifetime: { type: integer, default: 3600 }
```

```yaml
# after
configuration:
  schema:
    type: object
    properties:
      lifetime: { type: integer }
  defaults:
    lifetime: 3600
```

`defaults` and a Context override now layer rather than replace each other: a Context that names
one value leaves the rest of `defaults` standing, where before it unset them and the schema filled
them back in. A configuration a client **stores** through the values service still replaces the
whole object — it is the value, not a patch — so what is stored has to be whole.

A schema's `required` was often satisfied by its own defaults. Where that was so, the value has to
move into `defaults` or the component will not start:

```
Configuration must have required property 'lifetime'
```

## A criteria value the property cannot hold is refused

A criteria arrives as text, and the value was read as whatever the property's type made of it —
`parseInt` answers `NaN` for a word and `12` for `12kg`, and a storage asked to match either finds
nothing. A mistyped filter read as an empty result:

```http
GET /pots/?criteria=volume>abc

200 OK
[]
```

It is refused now, and says why:

```http
GET /pots/?criteria=volume>abc

400 Bad Request

Criteria selector 'volume' takes a number, and 'abc' is not one
```

What changes for a `number` or an `integer` property is that the whole value must read as one —
`1.5kg` no longer means `1.5`, and `1.5` is no longer an `integer`. For a `boolean`, only `true`
and `false` are accepted, where anything else used to mean `false`. A `string` property is
unchanged: every text is one.

## An unknown `query` property is refused

A query the runtime does not know a property of used to have it deleted on the way in. It is
refused now, as a contract error, so a misspelled one says so instead of doing nothing:

```
/query Property 'critera' is not expected to be here
```

Over HTTP the gateway refuses an unknown query parameter of its own, as before — and now also
refuses `omit` and `limit` where the route answers one object rather than a collection, which it
used to accept and drop:

```
400 Bad Request

Query limit is not allowed
```

`source` — what a call is stamped with, and what the introspection map draws an edge from — is no
longer held to a schema at all. Whatever a peer on another release puts beside the keys this one
knows travels through and is ignored, rather than failing the call.

## The SQL storage is removed

`@toa.io/storages.sql` is no longer published, the `sql` storage shortcut is gone, and a component
that declares `storage: sql` does not resolve. It never implemented `index` or `unique` and does
not implement migrations.

There is no replacement. An application on it either pins `1.0.0-alpha.284` of the connector and
keeps a copy of its own, or moves the component to MongoDB.

The `sql` Context annotation goes with it:

```yaml
# context.toa.yaml — remove
sql: pg://host0.example.com/production
```

## What to do

1. Rename the four properties wherever a record is read or written — an algorithm, an event, a
   receiver, a guard.
2. Rename them in every manifest: the entity `schema`, an `io:output` list, and a declared
   `criteria`, `sort` or `projection`. Indexes are declared in a migration — see
   [Indexes move into migrations](#indexes-move-into-migrations).
3. Rename them in the query strings a client sends.
4. Regenerate types with `toa types`.
5. Take the `svas` release that reads the new names, if a page of yours uses it.
6. Move every component's `entity.schema` up a level: `properties` and `required` beside
   `storage`, and nothing of the envelope — no `type`, no `additionalProperties`, no
   `definitions`.
7. Move a `default` written into an entity property into `entity.blank`, and drop one written
   into `VERSION`, `CREATED`, `UPDATED` or `DELETED`; the runtime writes all four.
8. Move a `default` written into a `configuration` schema into `configuration.defaults`.
9. Declare a `default` written into an operation's `input` at the call site, or make the property
   `required` and pass it.
10. Declare indexes and data changes as migrations, and drop `entity.index` and `entity.unique`.
11. Move a component off `@toa.io/storages.sql`, and drop the `sql` annotation from the Context.
12. Stop sending `omit` or `limit` to a route that answers one object, and check that every
    criteria value reads as the property it selects on.
13. Read the version of a created or updated record from the body, not from `etag`, and stop
    sending `if-none-match` for a reply that carries no `VERSION`.
14. Back the database up. A rollback has nothing else to stand on.
15. Stop every replica of the release before this one.
16. Deploy, then read `system_migrations` to see `system:0000-property-names` applied to each
    collection.
