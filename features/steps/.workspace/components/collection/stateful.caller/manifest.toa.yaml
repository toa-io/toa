name: caller
namespace: stateful

operations:
  relay:
    type: computation
    input:
      type: object
      properties:
        instance:
          type: string
        endpoint:
          type: string
        input: {}
        timeout:
          type: integer
      required: [instance, endpoint]
    output: {}
