namespace: dummies
name: associated

entity:
  associated: true
  schema:
    properties:
      a:
        type: integer
        default: 10
      b:
        type: string
    required:
      - a
      - b
    type: object

operations:
  assign:
    input:
      properties:
        a:
          type: string
          default: .
        b:
          type: string
          default: .
      type: object
